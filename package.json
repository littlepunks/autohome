{
  "name": "autohome",
  "version": "1.0.0",
  "private": true,
  "description": "AutoHome",
  "main": "autohome.js",
  "scripts": {
    "start": "sudo nodemon --ignore '*.html' --ignore '*.json' --ignore 'node_modules/*' autohome.js",
    "test": "sudo nodemon --ignore '*.html' --ignore '*.json' --ignore 'node_modules/*' autohome.js -- -t -d"
  },
  "author": "David Jacobsen",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "colors": "^1.4.0",
    "express": "^4.17.1",
    "serialport": "^9.0.0",
    "socket.io": "^2.3.0",
    "tplink-smarthome-api": "^2.0.0",
    "tuyapi": "^5.3.1",
    "util": "^0.12.3",
    "request": "^2.88.2"
  }
}
